# Example environment variables for local development / CI
# Set the public API URL the frontend will call (no secrets here).
# When building with Vite, use VITE_API_URL.

# Example pointing to local backend (dev):
VITE_API_URL=http://localhost:8000

# Example for production (set this in Render as an environment variable for the Static Site):
# VITE_API_URL=https://my-backend.onrender.com
